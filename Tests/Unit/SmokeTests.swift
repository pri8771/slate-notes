import XCTest

final class SmokeTests: XCTestCase {
    // Replaced by real logic tests as features land. Exists so the tests gate
    // is exercised from the very first CI run.
    func testTruth() {
        XCTAssertTrue(true)
    }
}
